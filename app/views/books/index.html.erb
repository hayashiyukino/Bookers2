<!-- ユーザーの詳細の部分テンプレート -->
<!--引数の@user,@bookはコントローラで定義する-->
<%= render 'users/UserInfo', user: @user, book: @book%>

<!--Book一覧-->
<!--<p id = "notice">%= flash[:notice] %></p>-->
<h2>Books</h2>
  <table>
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
      <tr>
        <td>
          <!--投稿したユーザー詳細画面へのリンクをつける-->
          　<!--リンク先-->
          <%= link_to user_path(book.user.id) do %> 
          　<!--ユーザーの画像-->
          <%= image_tag book.user.get_profile_image(100,100) %>
          <% end %>
        </td>
        <td>
          <%= link_to book_path(book.id) do %>
          <%= book.title %>
          <% end %>
        </td>
        <td><%= book.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

