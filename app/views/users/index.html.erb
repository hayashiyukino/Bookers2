<!-- ユーザーの詳細 -->
<%= render 'UserInfo', user: @user %>

<!-- ユーザーの一覧 -->

<% @users.each do |user| %>
<div>
  <%= link_to user_path(user.id) do %>
  <% end %>
  <p><%= image_tag user.get_profile_image(100,100) %></p>
  <p><%= user.name %></p>

</div>
<% end %>